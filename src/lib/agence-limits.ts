/** Cabinet & Groupement plan caps — shown on /agence/facturation and
 * enforced in the invite and devis-generation routes. Keep both in sync. */
export const ARTISAN_LIMIT = 50;
export const DEVIS_LIMIT_PER_MONTH = 500;
